#include "main.h"
#include <unistd.h>

/**
 * _strcat - concatenates two strings
 * @c: the character to be printed
 *
 * Return: 0n succes 1
 * and on error -1 is returned and the error is set appropriatelly
 */
int _putchar(char c)
{
	return(write(1, &c, 1);
}
