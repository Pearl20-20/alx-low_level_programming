#!/bin/bash
#include <stio.h>

/**
 * main - prints the phrase "programming is like building a multilingual puzzle 
 * Return: 0 if exited properly, non-zero ortherwise
 */
int main(void)
{
	puts("with proper grammer, but the outcome is a piece of art,\n);
	return(0);
}
